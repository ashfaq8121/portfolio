name = "portfolio-rate-limiter"
account_id = "8da4d3d2a433962e4bba9d3cc26bf686"
main = "src/index.ts"
compatibility_date = "2024-04-03"

[[durable_objects.bindings]]
name = "RATE_LIMITER"
class_name = "RateLimiter"

# Required the first time a Durable Object class is introduced — tells
# Cloudflare this class is new, so it provisions storage for it. This
# lives here (not in the main portfolio's wrangler.toml) because this
# Worker is the one that actually owns/exports the class.
[[migrations]]
tag = "v1"
new_classes = ["RateLimiter"]